#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateZoneInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateZoneInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE zone SET $update WHERE id=$ctx.args.updateZoneInput.id", "SELECT * FROM Zone WHERE id=$ctx.args.updateZoneInput.id"]
}