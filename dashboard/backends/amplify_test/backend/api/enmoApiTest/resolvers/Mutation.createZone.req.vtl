#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createZoneInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createZoneInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Zone $colStr VALUES $valStr", "SELECT * FROM Zone WHERE id=$ctx.args.createZoneInput.id"]
}