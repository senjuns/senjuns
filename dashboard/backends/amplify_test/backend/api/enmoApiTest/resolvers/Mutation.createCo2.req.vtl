#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCo2Input.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCo2Input[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Co2 $colStr VALUES $valStr", "SELECT * FROM Co2 WHERE id=$ctx.args.createCo2Input.id"]
}