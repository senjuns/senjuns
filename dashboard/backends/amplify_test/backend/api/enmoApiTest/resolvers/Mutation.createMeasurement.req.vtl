#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createMeasurementInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createMeasurementInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Measurement $colStr VALUES $valStr", "SELECT * FROM Measurement WHERE id=$ctx.args.createMeasurementInput.id"]
}