#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createGrowth_cycleInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createGrowth_cycleInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Growth_cycle $colStr VALUES $valStr", "SELECT * FROM Growth_cycle WHERE id=$ctx.args.createGrowth_cycleInput.id"]
}