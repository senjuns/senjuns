#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createLabel_taskInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createLabel_taskInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Label_task $colStr VALUES $valStr", "SELECT * FROM Label_task WHERE id=$ctx.args.createLabel_taskInput.id"]
}