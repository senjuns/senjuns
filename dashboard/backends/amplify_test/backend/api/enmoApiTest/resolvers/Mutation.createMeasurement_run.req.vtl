#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createMeasurement_runInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createMeasurement_runInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Measurement_run $colStr VALUES $valStr", "SELECT * FROM Measurement_run WHERE id=$ctx.args.createMeasurement_runInput.id"]
}