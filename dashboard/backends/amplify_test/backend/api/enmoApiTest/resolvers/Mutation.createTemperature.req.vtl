#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createTemperatureInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createTemperatureInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Temperature $colStr VALUES $valStr", "SELECT * FROM Temperature WHERE id=$ctx.args.createTemperatureInput.id"]
}