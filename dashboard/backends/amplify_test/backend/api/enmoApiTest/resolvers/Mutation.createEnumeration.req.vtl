#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createEnumerationInput.keySet() )
  #if ($entry != "id")
    #set ( $value = $ctx.args.createEnumerationInput[$entry] )
    #set( $discard = $cols.add($entry) )
    #set( $discard = $vals.add("'$ctx.args.createEnumerationInput[$entry]'") )
  #end
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Enumeration $colStr VALUES $valStr", 
  "SELECT * FROM Enumeration WHERE code='$ctx.args.createEnumerationInput.code' and type='$ctx.args.createEnumerationInput.type'"]
}