#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createTrackInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createTrackInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Track $colStr VALUES $valStr", "SELECT * FROM Track WHERE id=$ctx.args.createTrackInput.id"]
}