#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createConfigInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createConfigInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Config $colStr VALUES $valStr", "SELECT * FROM Config WHERE id=$ctx.args.createConfigInput.id"]
}