#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSystemInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSystemInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO System $colStr VALUES $valStr", "SELECT * FROM System WHERE id=$ctx.args.createSystemInput.id"]
}