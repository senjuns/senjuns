#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createDetectorInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createDetectorInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Detector $colStr VALUES $valStr", "SELECT * FROM Detector WHERE id=$ctx.args.createDetectorInput.id"]
}