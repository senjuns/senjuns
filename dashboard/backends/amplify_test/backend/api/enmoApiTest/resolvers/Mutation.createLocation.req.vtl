#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createLocationInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createLocationInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Location $colStr VALUES $valStr", "SELECT * FROM Location WHERE id=$ctx.args.createLocationInput.id"]
}