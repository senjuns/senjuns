#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPoseInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPoseInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Pose $colStr VALUES $valStr", "SELECT * FROM Pose WHERE id=$ctx.args.createPoseInput.id"]
}