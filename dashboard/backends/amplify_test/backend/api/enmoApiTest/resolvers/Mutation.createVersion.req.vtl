#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createVersionInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createVersionInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Version $colStr VALUES $valStr", "SELECT * FROM Version WHERE id=$ctx.args.createVersionInput.id"]
}