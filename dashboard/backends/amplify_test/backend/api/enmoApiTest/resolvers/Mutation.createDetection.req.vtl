#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createDetectionInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createDetectionInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Detection $colStr VALUES $valStr", "SELECT * FROM Detection WHERE id=$ctx.args.createDetectionInput.id"]
}