#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateParametersInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateParametersInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE parameters SET $update WHERE id=$ctx.args.updateParametersInput.id", "SELECT * FROM Parameters WHERE id=$ctx.args.updateParametersInput.id"]
}