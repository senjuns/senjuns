#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createOrganizationInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createOrganizationInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Organization $colStr VALUES $valStr", "SELECT * FROM Organization WHERE id=$ctx.args.createOrganizationInput.id"]
}